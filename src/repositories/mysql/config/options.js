const MYSQL_OPTIONS = {
    client: "mysql2",
    connection: {
      host: "localhost",
      user: "root",
      password: "",
      database: "workchat_db",
    },
}

module.exports ={
    MYSQL_OPTIONS
}